@model PropertyLeasing.API.Models.Unit
@{
    ViewData["Title"] = "Add Unit";
}

<div class="row justify-content-center">
    <div class="col-md-6">
        <h2 class="mb-1">Add Unit</h2>
        <p class="text-muted mb-3">Property: <strong>@ViewBag.PropertyName</strong></p>
        <div class="card shadow-sm">
            <div class="card-body">
                <form asp-action="CreateUnit" method="post">
                    <input type="hidden" asp-for="PropertyId" value="@ViewBag.PropertyId" />
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                    <div class="mb-3">
                        <label asp-for="UnitNumber" class="form-label"></label>
                        <input asp-for="UnitNumber" class="form-control" placeholder="e.g. 101" />
                        <span asp-validation-for="UnitNumber" class="text-danger"></span>
                    </div>
                    <div class="mb-3">
                        <label asp-for="RentAmount" class="form-label"></label>
                        <div class="input-group">
                            <span class="input-group-text">$</span>
                            <input asp-for="RentAmount" class="form-control" type="number" step="0.01" />
                        </div>
                        <span asp-validation-for="RentAmount" class="text-danger"></span>
                    </div>
                    <div class="mb-3">
                        <label asp-for="Size" class="form-label"></label>
                        <input asp-for="Size" class="form-control" placeholder="e.g. 850 sqft" />
                        <span asp-validation-for="Size" class="text-danger"></span>
                    </div>
                    <div class="mb-3">
                        <label asp-for="Amenities" class="form-label"></label>
                        <input asp-for="Amenities" class="form-control" placeholder="e.g. AC, Parking, Pool" />
                        <span asp-validation-for="Amenities" class="text-danger"></span>
                    </div>
                    <div class="mb-3 form-check">
                        <input asp-for="IsAvailable" class="form-check-input" />
                        <label asp-for="IsAvailable" class="form-check-label">Available for Rent</label>
                    </div>

                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-primary">Add Unit</button>
                        <a asp-action="Units" asp-route-propertyId="@ViewBag.PropertyId" class="btn btn-outline-secondary">Cancel</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
